import { createSlice } from '@reduxjs/toolkit';

export const initialState = {};

export const newProductSlice = createSlice({
  name: 'new-product',
  initialState,
  reducers: {},
});

export const {} = newProductSlice.actions;
